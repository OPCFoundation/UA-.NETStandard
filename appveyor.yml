#---------------------------------#
#      general configuration      #
#---------------------------------#

# you can use {branch} name in version format too
version: 1.0.{build}-{branch}

# branches to build
branches:
  # whitelist
  only:
    - appveyor

  # blacklist
  except:
    - gh-pages